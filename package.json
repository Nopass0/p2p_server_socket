{
  "name": "p2p_server_socket",
  "module": "src/app.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/app.ts",
    "build": "bun run build",
    "start": "bun run src/app.ts",
    "postinstall": "bunx prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@prisma/client": "6.1.0",
    "axios": "^1.7.9",
    "prisma": "^6.1.0"
  }
}
